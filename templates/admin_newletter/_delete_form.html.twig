<form method="post" action="{{ path('app_admin_newletter_delete', {'id': newletter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newletter.id) }}">
    <button class="btn">Delete</button>
</form>
