{% extends 'base-backend.html.twig' %}

{% block title %}Partenaire {% endblock %}

{% block body %}
    <h1 class="text-center">Partenaire </h1>

    <a href="{{ path('app_admin_partenaire_new') }}" class="btn btn-dark my-3">Nouveau</a>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Image</th>
                {# <th>UpdatedAt</th> #}
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partenaire in partenaires %}
            <tr>
                <td>{{ partenaire.id }}</td>
                <td> {% if partenaire.imageName is defined and partenaire.imageName is not null %}
                    <img src="{{ asset('images/partenaires/' ~ partenaire.imageName) }}" alt="" class="img-list">
                </a>
                {% else %}
                N/A                               
            {% endif %}</td>
                {# <td>{{ partenaire.updatedAt ? partenaire.updatedAt|date('Y-m-d H:i:s') : '' }}</td> #}
                <td>{{ partenaire.nom }}</td>
                <td>
                    <a href="{{ path('app_admin_partenaire_show', {'id': partenaire.id}) }}"title="Voir" class="btn btn-dark">
                <i class="align-middle" data-feather="eye"></i></a>
                    <a href="{{ path('app_admin_partenaire_edit', {'id': partenaire.id}) }}"title="Modifier" class="btn btn-success ms-2">
                <i class="align-middle" data-feather="edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun partenaire trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
