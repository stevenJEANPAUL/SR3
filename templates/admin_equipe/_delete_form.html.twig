<form method="post" action="{{ path('app_admin_equipe_delete', {'id': equipe.id}) }}" onsubmit="return confirm('Supprimer équipe ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.id) }}">
    <button title="Supprimer" class="btn btn-danger mt-3"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
