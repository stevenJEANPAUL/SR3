<form method="post" action="{{ path('app_admin_formation_delete', {'id': formation.id}) }}" onsubmit="return confirm('Suppression de toute la page ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id) }}">
<button title="Supprimer" class="btn btn-danger mt-3"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
