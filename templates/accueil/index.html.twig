{% extends 'base.html.twig'%}

{% block title %}SR3GROUPE - Accueil{% endblock %}

{% block stylesheets %}

<!-- CSS only -->
{# <link href="https://cdn.jsdelivr.net/npm/bootstrap5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous"> #}
<link rel="stylesheet" href="{{ asset('css/accueil.css')}}">
{% endblock %}

{% block body %}
<main class="container-md-fluid">

  <div class="hautpage">
    <img src="{{ asset("images/accueil/" ~ monAccueil.imageName) }}" alt="" class="img-fluid presentation">
    <div class="text-over text-start">{{ monAccueil.description|raw}}</div> 
  </div>
{#partie mobile de la presentation#}

<div class="hautmob">

<div class="mob-texte">{{ monAccueil.description|raw}}</div> 

</div>
{# fin de la partie mobile de la presetation#}

  <h2>{{ monAccueil.titre}}</h2>
    {# --carousel nos domaines de formations--- #}
  <div class="carou">

    {% if monAccueil.carousels|length > 0 %}
      <div id="carouselExampleDark" class="carousel carousel-dark slide text-white" data-bs-ride="carousel">
    
        <div class="carousel-indicators">
          {% for carousel in monAccueil.carousels  %}
          {% if loop.index0 == 0 %}
          {% set active = "active" %}
          {% else %}
          {% set active = "" %}
          {% endif %}
          <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="{{ loop.index0 }}" class="{{ active }}" aria-current="true" aria-label="Slide {{ loop.index }}"></button>
          {% endfor %}
        </div>

        <div class="carousel-inner">
          {% for carousel in monAccueil.carousels  %}
          {% if loop.index0 == 0 %}
          {% set active = "active" %}
          {% else %}
          {% set active = "" %}
          {% endif %}
          <div class="carousel-item {{ active }}" data-bs-interval="4500">
            <img id="pho1" src="{{ asset('images/carousels/' ~ carousel.imageName) }}" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block caroutexte">
              <div class="caroutexte">
                {{ carousel.texte|raw}}
              </div>
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
    {% endif %}
  </div>
{# --fin carousel nos domaines de formations--- #}

  {# début de la partie mobile#}
  <div class="row" id="mobicon"> 
  <div class=" text-center col-md-6 centt">
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/bureau.png') }}"><a>                                         
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/programming.png') }}"><a> 
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/project-management.png') }}"><a> 
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/croissance.png') }}"><a>
    </div>
    <div class="text-center col-md-6 centt">                                        
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/reseau-mondial.png') }}"><a> 
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/la-cyber-securite.png') }}"><a> 
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/programmation.png') }}"><a> 
    <a href="{{ path('app_front_formation') }}"><img class="nav-item my-5 mx-5 " style="width: 5rem;" src="{{ asset ('images/formation/virtualisation.png') }}"><a>
    </div>  
    
</div>   
  {# fin de la partie mobile #}

{# ----- #}

                <h2>{{ monAccueil.nom}}</h2>

{# --avis de nos clients--- #}
    {# {% if monAccueil.avisClients|length > 0 %}
    <div id="carouselExampleDark1" class="carousel carousel-dark slide bum" data-bs-ride="carousel">
      <div class="carousel-indicators">
      {% for avisClient in monAccueil.avisClients  %}
                {% if loop.index0 == 0 %}
                {% set active = "active" %}
                {% else %}
                {% set active = "" %}
                {% endif %}
    
        <button type="button" data-bs-target="#carouselExampleDark1" data-bs-slide-to="{{ loop.index0 }}" class="{{ active }} py-1 butt" aria-current="true" aria-label="{{ loop.index }}" style="border: 2px solid red"></button>
      {% endfor %}
      </div>
    
      <div class="carousel-inner">
      {% for avisClient in monAccueil.avisClients  %}
                {% if loop.index0 == 0 %}
                {% set active = "active" %}
                {% else %}
                {% set active = "" %}
                {% endif %}
    
      <div class="carousel-item {{ active }} juste" data-bs-interval="false">

        <div class="card justify-content-center" style="width: 12rem;">
          <img src="{{ asset('images/avisClient/' ~ avisClient.imageName) }}" class="card-img-top ms-5"  alt="..."style="width: 5rem;">

          <div class="card-body">
            <h5 class="card-title" style="font-size: 1rem;">{{ avisClient.nom}}</h5>
            <div style="font-size: 0.8rem;">{{ avisClient.poste}}</div>
          </div>

        </div>
            <div class="avcli">
            <div class="card-text me-auto">
              {{ avisClient.commentaire|raw}}
            </div>
            </div>
      </div>
        {% endfor %}
    
      </div>
    </div>
    {% endif %} #}


{% if monAccueil.avisClients|length > 0 %}
<div id="carouselExampleCaptions" class="carousel slide primo" data-bs-ride="false">
  <div class="carousel-indicators">

  {% for avisClient in monAccueil.avisClients %}
  {% if loop.index0 == 0 %}
  {% set active = "active" %}
  {% else %}
  {% set active = "" %}
  {% endif %}
  <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="{{ loop.index0 }}"  class="{{ active }} p-1 butt" aria-current="true" aria-label="{{ loop.index }}" style="border: 3px solid red"></button>
  {% endfor %}
</div>

<div class="carousel-inner  seco row">
  {% for avisClient in monAccueil.avisClients %}
  {% if loop.index0 == 0 %}
  {% set active = "active" %}
  {% else %}
  {% set active = "" %}
  {% endif %}
  <div class="carousel-item {{ active }} col-12 col-md-3 col-lg-4 col-xl-3 poste" >
    <div class="card justify-content-center clis" style="width: 12rem;">
      <img src="{{ asset('images/avisClient/') ~ avisClient.imageName }}" class="card-img-top ms-5" style="width: 6rem;" alt="...">
      <div class="card-body text-center">
        <h5 class=" ">{{ avisClient.nom}}</h5>
        <p class="poste ">{{ avisClient.poste}}</p>
      </div>  
      </div>
      <div class="avis">
        <div class="card-text comment">
        {{ avisClient.commentaire|raw}}
        </div>
      </div>
  </div>

  {% endfor %}
</div>

{# <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
<span class="carousel-control-prev-icon" aria-hidden="true"></span>
<span class="visually-hidden">Previous</span>
</button>
<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
<span class="carousel-control-next-icon" aria-hidden="true"></span>
<span class="visually-hidden">Next</span>
</button> #}
</div>
{% endif %} 
    
{# -- fin avis de nos clients--- #}


<h2>{{ monAccueil.commentaire}}</h2>
{# ----début partenaire-- #}
  
    <div class="message text-center">{{ monAccueil.texte|raw}}</div>

    <div class="row" id="partena">
      {% for partenaire in monAccueil.partenaires %}
      <div class="text-center col-12 col-md-4 col-lg-4 col -xl-3 parten">
        <div class="">
          <img src="{{ asset('images/partenaires/' ~ partenaire.imageName) }}" alt="" class="img-fluid mt-4" style="width: 8rem;" id="parte">
        </div>

      </div>

      {% endfor %}
    </div>
  <div class="text-center mobil">{{ monAccueil.texte|raw}}</div>
{# ---fin de partenaire--- #}

</main> 

{% block javascripts %}
    <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
{% endblock %}
{% endblock %}