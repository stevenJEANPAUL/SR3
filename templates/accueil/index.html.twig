{% extends 'base.html.twig' %}

{% block title %}SR3GROUPE - Accueil{% endblock %}

{% block stylesheets %}

<!-- CSS only -->
{# <link href="https://cdn.jsdelivr.net/npm/bootstrap5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous"> #}
<link rel="stylesheet" href="{{ asset('css/accueil.css')}}">
{% endblock %}

{% block body %}
<main class="container-md-fluid">

  <div class="presentaion1">
    <img src="{{ asset("images/accueil/" ~ monAccueil.imageName) }}" alt="" class="presentation">
    <p class="text-over">{{ monAccueil.description}}</p> 
  </div>


  <h2>{{ monAccueil.titre}}</h2>
    {# --carousel nos domaines de formations--- #}
  <div class="carou">

    {% if monAccueil.carousels|length > 0 %}
      <div id="carouselExampleDark" class="carousel carousel-dark slide" data-bs-ride="carousel">
    
        <div class="carousel-indicators">
          {% for carousel in monAccueil.carousels  %}
          {% if loop.index0 == 0 %}
          {% set active = "active" %}
          {% else %}
          {% set active = "" %}
          {% endif %}
          <button type="button" data-bs-target="#carouselExampleDark" data-bs-slide-to="{{ loop.index0 }}" class="{{ active }}" aria-current="true" aria-label="Slide {{ loop.index }}"></button>
          {% endfor %}
        </div>

        <div class="carousel-inner">
          {% for carousel in monAccueil.carousels  %}
          {% if loop.index0 == 0 %}
          {% set active = "active" %}
          {% else %}
          {% set active = "" %}
          {% endif %}
          <div class="carousel-item {{ active }}" data-bs-interval="7000">
            <img id="pho1" src="{{ asset('images/carousels/' ~ carousel.imageName) }}" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
              <p class="text-light caroutexte">
                {{ carousel.texte|raw}}
              </p>
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
    {% endif %}
  </div>
{# --fin carousel nos domaines de formations--- #}

  {# début de la partie mobile#}
  <div class="row justify-content-center mobicon" id=""> 
  <div class="col-6 my-4 mx-3 p-2 centt">
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/bureau.png') }}"><a>                                         
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/programming.png') }}"><a> 
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/project-management.png') }}"><a> 
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/croissance.png') }}"><a>                                         
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/reseau-mondial.png') }}"><a> 
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/la-cyber-securite.png') }}"><a> 
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/programmation.png') }}"><a> 
    <a href=""><img class="nav-item my-5 mx-3 " style="width: 4rem;" src="{{ asset ('images/formation/virtualisation.png') }}"><a> 
    </div>
</div>   
  {# fin de la partie mobile #}

{# ----- #}

                <h2>{{ monAccueil.nom}}</h2>

{# --avis de nos clients--- #}
    {% if monAccueil.avisClients|length > 0 %}
    <div id="carouselExampleDark1" class="carousel carousel-dark slide bum" data-bs-ride="carousel">
      <div class="carousel-indicators">
      {% for avisClient in monAccueil.avisClients  %}
                {% if loop.index0 == 0 %}
                {% set active = "active" %}
                {% else %}
                {% set active = "" %}
                {% endif %}
    
        <button type="button" data-bs-target="#carouselExampleDark1" data-bs-slide-to="{{ loop.index0 }}" class="{{ active }} butt " aria-current="true" aria-label="{{ loop.index }}"></button>
      {% endfor %}
      </div>
    
      <div class="carousel-inner">
      {% for avisClient in monAccueil.avisClients  %}
                {% if loop.index0 == 0 %}
                {% set active = "active" %}
                {% else %}
                {% set active = "" %}
                {% endif %}
    
      <div class="carousel-item {{ active }} juste" data-bs-interval="false">
        <div class="card" style="width: 12rem;">
          <img src="{{ asset('images/avisClient/' ~ avisClient.imageName) }}" class="card-img-top"  alt="...">
          <div class="card-body">
            <h5 class="card-title">{{ avisClient.nom}}</h5>
            <p>{{ avisClient.poste}}</p>
          </div>

        </div>
            <div class="avcli">
            <p class="card-text ">
              {{ avisClient.commentaire|raw}}
            </p>
            </div>
      </div>
        {% endfor %}
    
      </div>
    </div>
    {% endif %}
{# -- fin avis de nos clients--- #}


<h2 class="posi">{{ monAccueil.commentaire}}</h2>

    <div><p>{{ monAccueil.texte|raw}}</p></div>
{# ----début partenaire-- #}
    <div class="row mt-5" id="partena">
      {% for partenaire in monAccueil.partenaires %}
      <div class="col-4 col-md- col-lg-4 d-flex justify-content-around  my-5">
        <div class="">
          <img src="{{ asset('images/partenaires/' ~ partenaire.imageName) }}" alt="img-fluid" class="" id="parte">
        </div>

      </div>

      {% endfor %}
    </div>
{# ---fin de partenaire--- #}
</main> 

{% block javascripts %}
    <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
{% endblock %}
{% endblock %}