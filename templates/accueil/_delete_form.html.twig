<form method="post" action="{{ path('app_accueil_delete', {'id': accueil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accueil.id) }}">
    <button class="btn">Delete</button>
</form>
