<form method="post" action="{{ path('app_admin_avis_client_delete', {'id': avis_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_client.id) }}">
    <button class="btn">Delete</button>
</form>
